/**
 * app/page.tsx
 * Home page — renders the main converter dashboard.
 */

import ConverterDashboard from '@/components/ConverterDashboard';

export default function HomePage() {
  return <ConverterDashboard />;
}
